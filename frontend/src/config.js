/**
 * Application Configuration
 */

// App Name (used in Header, Footer, etc.)
export const APP_NAME = 'Arcade Haven';

// API Base URL
// For development, if using CRA proxy, this can be an empty string or relative path.
// For production, this will be the deployed backend URL.
export const API_BASE_URL = process.env.REACT_APP_API_BASE_URL || '';

// Other global settings can be added here
// export const MAX_LEADERBOARD_SCORES = 10;
